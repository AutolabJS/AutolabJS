sudo: required
dist: trusty

language: node_js
node_js:
  - "6"

services: 
 - docker
 - mysql
 
install:
 - pip install ansible

before_script:
 - npm install
 - cd ./deploy/
 - bash setup.sh
 
script:
 - sudo ansible-playbook -i inventory playbook.yml
