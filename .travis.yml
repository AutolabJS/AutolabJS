sudo: required
dist: trusty

language: node_js
node_js:
  - "6"

services:
 - mysql
 
install:
 - pip install --upgrade pip

before_script:
 - npm install -g jshint
 - npm install
 
script:
 - bash test.sh
