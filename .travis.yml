sudo: required
dist: trusty

language: node_js
node_js:
  - "6"

services:
 - mysql

before_script:
 - npm install -g jshint
 - npm install -g eslint
 - npm install -g autolabcli
 - mysql -e 'CREATE DATABASE Autolab;'
 - echo "USE mysql;\nUPDATE user SET password=PASSWORD('autolab') WHERE user='root';\nFLUSH PRIVILEGES;\n" | mysql -u root 
 
script:
 - bash test.sh
