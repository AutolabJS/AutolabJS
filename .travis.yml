sudo: required
dist: trusty

language: node_js
node_js:
  - "6"
 
install:
 - pip install --upgrade pip
 - pip install ansible

before_script:
 - npm install
 - cd ./deploy/
 - bash setup.sh
 
script:
 - ansible-playbook -i inventory playbook.yml
