---
- name: Getting docker script
  get_url: 
    url: https://get.docker.com/
    dest: '{{ playbook_dir }}/helper_scripts/tempdocker.sh'
    mode: 0775

- name: Running docker script
  shell: '{{ playbook_dir }}/helper_scripts/tempdocker.sh'

- name: Delete tempdocker.sh
  file:
    path: "{{ item }}"
    state: absent
  with_items:
    - '{{ playbook_dir }}/helper_scripts/tempdocker.sh'

- name: Restarting the docker service
  service:
    name: docker
    state: restarted
